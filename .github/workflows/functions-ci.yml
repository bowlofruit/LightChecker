name: LightChecker Functions

on:
  push:
    paths:
      - "LightChecker/functions/**"
      - ".github/workflows/functions-ci.yml"
  pull_request:
    paths:
      - "LightChecker/functions/**"

jobs:
  ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: LightChecker/functions
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          # Match the deployed Functions runtime (Node 20), not a newer major.
          node-version: "20"
          cache: npm
          cache-dependency-path: LightChecker/functions/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npm run lint
      - run: npm test
